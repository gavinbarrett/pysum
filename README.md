# pysum
This is a small script for verifying linux .iso images. Simply run the script followed by the checksum file containing the sha256 of the file followed by the .iso image itself. If the image is good, the script will print 'Good checksum match!'.
